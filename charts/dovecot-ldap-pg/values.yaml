# Default values for dovecot-ldap-pg.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: 10.43.36.98:5000/armin-felder-gmail-com/dovecot-ldap-pg
  tag: 0.0.2
  pullPolicy: IfNotPresent
service:
  name: dovecot-ldap-pg
  type: ClusterIP
  externalPort: 465
  internalPort: 465
  annotations:
    fabric8.io/expose: "true"
    fabric8.io/ingress.annotations: "kubernetes.io/ingress.class: nginx"
resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi
ingress:
  enabled: false
